	public BigDecimal getVtlin(BigDecimal vtm) {
		List<HashMap<String, BigDecimal>> list = this.getOrgDataSet01Idvb();
		HashMap<String, BigDecimal> val1 = getMaxValueLessThen(list, vtm);
		HashMap<String, BigDecimal> val2 = getMinValueGreaterThen(list, vtm);
		System.out.println(val1);
		System.out.println(val2);
		// vgs1+(의뢰서VTM-idvb1)*(vgs2-vgs1)/(idvb2-idvb1)
		BigDecimal tmp1 = val1.get("vgs");
		BigDecimal tmp2 = vtm.subtract(val1.get("idvb"));
		BigDecimal tmp3 = val2.get("vgs").subtract(val1.get("vgs"));
		BigDecimal tmp4 = val2.get("idvb").subtract(val1.get("idvb"));
		return tmp2.multiply(tmp3).divide(tmp4, RoundingMode.HALF_UP).add(tmp1);
	}
	
	/**
	 * vtm보다 작은 값 중 제일 큰 값을 반환한다.
	 * @param vtm
	 */
	private HashMap<String, BigDecimal> getMaxValueLessThen(List<HashMap<String, BigDecimal>> list, BigDecimal vtm) {
		List<HashMap<String, BigDecimal>> clone = new ArrayList<HashMap<String, BigDecimal>>();
		clone.addAll(list);
		Collections.reverse(clone);
		Iterator<HashMap<String, BigDecimal>> iter = clone.iterator();
		HashMap<String, BigDecimal> item = null;
		while (iter.hasNext()) {
			HashMap<String, BigDecimal> tmp = iter.next();
			if (vtm.compareTo(tmp.get("idvb")) > 0) {
				item = tmp;
				break;
			}
		}
		return item;
	}
	
	/**
	 * vtm보다 큰 값 중 제일 작은 값을 반환한다.
	 * @param vtm
	 */
	private HashMap<String, BigDecimal> getMinValueGreaterThen(List<HashMap<String, BigDecimal>> list, BigDecimal vtm) {
		Iterator<HashMap<String, BigDecimal>> iter = list.iterator();
		HashMap<String, BigDecimal> item = null;
		while (iter.hasNext()) {
			HashMap<String, BigDecimal> tmp = iter.next();
			if (vtm.compareTo(tmp.get("idvb")) < 0) {
				item = tmp;
				break;
			}
		}
		return item;
	}
	
	public static void main(String[] args) {
		TrDataTranform instance = new TrDataTranform();
		List<HashMap<String, BigDecimal>> resultList = instance.getDxdyDataSet01(instance.getOrgDataSet01());
		Iterator<HashMap<String, BigDecimal>> iter = resultList.iterator();
		while(iter.hasNext()) {
			HashMap<String, BigDecimal> item = iter.next();
			System.out.println("VGS: " + item.get("vgs") + ", IDVG_DXDY: " + item.get("idvb_dxdy"));
		}
		
		System.out.println(instance.replaceVtm("123nA*W/L"));
		System.out.println(instance.replaceVtm("321nA"));
		System.out.println(instance.replaceVtm("Vtext"));
		
		
		HashMap<String, BigDecimal> data = instance.getGmmax(resultList);
		System.out.println("Gmmax: " + data.get("idvb_dxdy"));
		System.out.println("vg_gmmax: " + data.get("vgs"));
		
		BigDecimal idGmmax = instance.getIdGmmax(instance.getOrgDataSet01(), data.get("vgs")).get("idvb");
		System.out.println("id_gmmax: " + idGmmax);
		
		System.out.println(instance.getVtlin(new BigDecimal("3.3888889e-8")));
	}
